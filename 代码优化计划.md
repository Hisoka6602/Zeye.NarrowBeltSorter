# 代码优化计划

> 本文档基于对仓库全量源代码的通读与分析，归纳出现存的多余代码、逻辑问题、冗余代码、性能问题与未落地日志，并给出逐条优化建议。问题按**优先级**排序（🔴 高 / 🟡 中 / 🟢 低）。

---

## 一、多余代码（Dead Code）

### DC-01 🔴 `Console.WriteLine` 调试遗留代码

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Services/CarrierLoopGroupingHostedService.cs` |
| 行号 | 130 |
| 问题 | `Console.WriteLine("建环完成")` 是调试阶段遗留代码。生产环境控制台输出不落盘、不受 NLog 路由管控，且该关键业务事件（建环完成）等同于无日志记录。 |
| 优化建议 | 删除 `Console.WriteLine`，改用 `_logger.LogInformation("建环完成，小车数量={Count}", ...)` 确保日志落盘。 |

---

### DC-02 🟡 `GetCurrentCarrierId` 无实际意义的包装方法

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Services/CarrierLoopGroupingHostedService.cs` |
| 行号 | 194–196 |
| 问题 | `GetCurrentCarrierId(int triggerIndex)` 方法体仅为 `return triggerIndex`，是对 `_carrierTriggerCount` 的无意义单层包装，调用处（行 150）完全可以直接使用 `_carrierTriggerCount`。 |
| 优化建议 | 删除该方法，调用处直接使用字段，提升可读性。 |

---

### DC-03 🟡 `_cachedCarrierIndexMap` 等缓存字段与实际调用路径脱节

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Services/SortingTaskDropOrchestrationService.cs` |
| 行号 | 43–53（字段定义）、88（调用 `GetOrderedCarrierIds`）、391–410（`GetOrBuildCarrierIndexMap`） |
| 问题 | `_cachedCarrierIndexMap`、`_cachedCarrierIndexSignature`、`_cachedCarrierIndexCount` 三个字段及 `GetOrBuildCarrierIndexMap` 方法只在 `DetectApproachingTargetChute` 内部（行 321）使用缓存，而 `HandleCurrentInductionCarrierChangedAsync` 的主调用路径（行 88）每次都调用 `GetOrderedCarrierIds()` 重新构建，缓存机制并未被热路径复用，造成逻辑混乱。 |
| 优化建议 | 将 `GetOrderedCarrierIds()` 的结果直接传入 `DetectApproachingTargetChute` 和 `DetectMissedChute`，统一用 `GetOrBuildCarrierIndexMap` 缓存代替每次重新排序，彻底消除热路径中冗余的 `GetOrderedCarrierIds` 调用。 |

---

### DC-04 🟢 `ParcelManager.AddParcel` 的冗余 null 条件运算符

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Parcel/ParcelManager.cs` |
| 行号 | 61 |
| 问题 | `parcel?.ParcelId ?? 0` 中 `?.` 是多余的 null 保护——代码在前面已经判断 `parcel is null` 并提前返回，到达此处说明 `parcel` 不为 null。 |
| 优化建议 | 改为 `parcel.ParcelId`，消除多余的 null 条件判断。 |

---

### DC-05 🟢 `ZhiQianChuteManager.ChuteConfigurationSnapshot` 属性对不可变字典加锁

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Drivers/Vendors/ZhiQian/ZhiQianChuteManager.cs` |
| 行号 | 112–115 |
| 问题 | `ChuteConfigurationSnapshot` 属性的 getter 对 `_chuteToDoMap` 加 `lock`，但 `_chuteToDoMap` 是构造时初始化的 `Dictionary<int, int>`，之后从未被修改，锁保护是无意义的。 |
| 优化建议 | 移除 `lock(_chuteToDoMap)` 包装，直接返回 `_chuteToDoMap.ToDictionary(...)` 即可（或改用在构造时预先缓存好的只读快照）。 |

---

### DC-06 🟢 `InfraredSensorCarrierManager._loadedCarrierIds` 永远为空集合

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Services/Carrier/InfraredSensorCarrierManager.cs` |
| 行号 | 73–79 |
| 问题 | `LoadedCarrierIds` 属性暴露了 `_loadedCarrierIds`，但全仓库中 `_loadedCarrierIds` 从未被 `Add` 或 `Remove` 操作修改，始终为空集合，属于 dead field，对调用方无实际意义。 |
| 优化建议 | 若该功能尚未实现，应删除字段和属性定义，或补充实现逻辑；避免对外暴露永远为空的集合误导调用方。 |

---

## 二、逻辑问题

### LG-01 🔴 `OnSystemStateChanged` 无条件清空建环数据

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Services/CarrierLoopGroupingHostedService.cs` |
| 行号 | 213–221 |
| 问题 | 任意系统状态变化（包括 `Running → StartupWarning` 等非关键转变）都会无条件清空 `_currentRingCarrierIds`、`_builtRingCarrierIds`、`_carrierTriggerCount` 等建环数据。这意味着建环成功后只要系统状态发生任何变化，就会丢失已建好的环形拓扑，导致后续落格计算失效。 |
| 优化建议 | 添加条件判断，只在系统进入停机类状态（如 `Stopped`、`EmergencyStop`）时才重置建环数据；`Running`、`StartupWarning` 等中间状态不应触发重置。 |

---

### LG-02 🔴 `ParcelManager.RaiseSafe` 使用同步串行 `Execute` 违反事件发布规范

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Parcel/ParcelManager.cs` |
| 行号 | 386–394 |
| 问题 | `RaiseSafe` 通过 `_safeExecutor.Execute`（同步、串行）调用所有订阅者，即所有事件订阅者被逐一同步执行，阻塞发布线程且订阅者之间相互串行等待。这违反了项目规范"事件发布必须采用非阻塞隔离执行""所有订阅者必须并行获取"。 |
| 优化建议 | 将 `_safeExecutor.Execute(...)` 改为 `_safeExecutor.PublishEventAsync(handler, this, args, "ParcelManager.EventDispatch")`，实现并行非阻塞发布，与其他模块保持一致。 |

---

### LG-03 🟡 `StopAsync` 与 `ExecuteAsync finally` 重复调用 `UnsubscribeEvents`

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Services/SortingTaskOrchestrationService.cs` |
| 行号 | 135（`finally` 中）、141（`StopAsync` 中） |
| 问题 | 正常停止路径下 `UnsubscribeEvents()` 会被调用两次（`StopAsync` 触发令牌取消 → `ExecuteAsync` finally 退订 + `StopAsync` 也退订）。虽然 null 守卫使其不会出错，但存在冗余调用，且调试时容易误判退订次数。`CarrierLoopGroupingHostedService`（行 89、97）存在相同问题。 |
| 优化建议 | `StopAsync` 中只释放信号量（触发 `OperationCanceledException`），由 `ExecuteAsync` 的 `finally` 统一退订，删除 `StopAsync` 中的冗余 `UnsubscribeEvents()` / `TryUnsubscribeAll()` 调用。 |

---

### LG-04 🟡 `PumpRawQueueAsync` 中 Peek/Delay/Dequeue 间缺乏原子性

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Services/SortingTaskOrchestrationService.cs` |
| 行号 | 194–205 |
| 问题 | 先 `TryPeek` 获取 `headParcel` 并计算等待时间，`await Task.Delay` 期间若队列被其他消费者（或逻辑错误）修改，再次 `TryDequeue` 可能取出不同包裹，成熟时间计算将产生错位。 |
| 优化建议 | 在 `TryDequeue` 后重新校验成熟时间，或在 Delay 后再次 Peek 确认是同一包裹，确保成熟时间与出队包裹一一对应。 |

---

### LG-05 🟡 `ZhiQianChuteManager` 多重锁嵌套顺序不统一，存在潜在死锁

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Drivers/Vendors/ZhiQian/ZhiQianChuteManager.cs` |
| 行号 | 285、308（`lock(_lockedChuteIds)` 单独使用）、346–361（`lock(_writeLock)` 内嵌套 `lock(_lockedChuteIds)`） |
| 问题 | `SetChuteLockedAsync` 持有 `_writeLock` 的同时嵌套 `lock(_lockedChuteIds)`，而其他方法持有 `_lockedChuteIds` 时不持有 `_writeLock`。当前路径未产生死锁，但锁层次不清晰，后续维护时容易引入"先持 `_lockedChuteIds` 再申请 `_writeLock`"的逆序调用，造成死锁。 |
| 优化建议 | 明确锁层次规范（例如"始终先申请 `_writeLock` 再申请 `_lockedChuteIds`"），或将 `_lockedChuteIds` 改用并发集合（`ConcurrentDictionary`/`ImmutableHashSet` 写时替换）消除嵌套锁。 |

---

### LG-06 🟡 `DetectMissedChute` 两段遍历之间缺乏原子性，可能产生虚假"错过格口"

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Services/SortingTaskDropOrchestrationService.cs` |
| 行号 | 265–307 |
| 问题 | 第一段遍历 `_carrierAtTargetStates` 收集过期小车，第二段遍历 `_carrierLoadingService.CarrierParcelMap` 更新状态，两段之间无锁，并发修改 `CarrierParcelMap` 时可能导致 `wasAtTarget=true` 但 `isAtTarget=false` 的虚假判定，发出误报的"错过格口"警告。 |
| 优化建议 | 两段遍历使用相同快照（在进入方法前对 `CarrierParcelMap` 做一次快照），或通过适当同步手段确保一致性。 |

---

### LG-07 🟢 `IoMonitoringHostedService.StopAsync` 无异常保护

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Services/Hosted/IoMonitoringHostedService.cs` |
| 行号 | 95–100 |
| 问题 | `StopAsync` 中连续调用三个 await（StopMonitoringAsync × 2 + DisposeAsync），任意一步抛出异常都会中断后续停止流程，导致资源泄漏且无日志记录，与同文件的 `CleanupAfterStartupFailureAsync` 的逐步 try-catch 处理方式不一致。 |
| 优化建议 | 参照 `CleanupAfterStartupFailureAsync` 对每步单独 try-catch 并记录错误日志，确保停止链路中的异常不影响其他步骤执行。 |

---

### LG-08 🟢 `LeadshaineEmcController.SetStatus` 状态切换无日志

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Drivers/Vendors/Leadshaine/Emc/LeadshaineEmcController.cs` |
| 行号 | 428–441 |
| 问题 | `SetStatus` 仅发布 `StatusChanged` 事件，不记录任何日志。若事件订阅者缺失或异常，EMC 控制器状态机（Connected → Disconnected → Faulted 等关键转换）将无迹可查，违反"所有异常路径都必须记录日志并落盘"规范。 |
| 优化建议 | 在发布事件前添加 `_logger.LogInformation("EMC 状态变更: {OldStatus} → {NewStatus}，原因: {Reason}", oldStatus, status, reason)`，确保状态转换始终有日志记录。 |

---

## 三、冗余代码（影分身代码）

### RD-01 🔴 `WrapIndex` 方法在两个文件中完全重复实现

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Services/SortingTaskDropOrchestrationService.cs`（行 246–254）`Zeye.NarrowBeltSorter.Execution/Services/Carrier/InfraredSensorCarrierManager.cs`（行 266–274） |
| 问题 | 两个文件中 `WrapIndex(int index, int length)` 实现完全一致，违反"相同意义的工具代码需要提取集中"规范，且仓库中已存在 `CircularValueHelper` 工具类，可直接扩展。 |
| 优化建议 | 在 `Zeye.NarrowBeltSorter.Core/Utilities/CircularValueHelper.cs` 中添加 `public static int WrapIndex(int index, int length)` 方法，两处调用统一改用该工具方法，并删除各自的私有副本。 |

---

### RD-02 🟡 `StopMonitoringAsync` 停止逻辑在 `LeadshaineIoPanel` 与 `LeadshaineSensorManager` 中高度重复

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Drivers/Vendors/Leadshaine/IoPanel/LeadshaineIoPanel.cs`（行 130–148）`Zeye.NarrowBeltSorter.Drivers/Vendors/Leadshaine/Sensors/LeadshaineSensorManager.cs`（行 117–145） |
| 问题 | 两者的 `StopMonitoringAsync` 结构几乎完全相同：Cancel CTS → Await monitoring task → Dispose CTS → SetStatus → Log。`LeadshaineIoPanel.DisposeAsync` 内还有第三份内联的相同模式，构成典型的影分身代码。 |
| 优化建议 | 提取公共抽象基类或静态辅助方法（如 `MonitoringLifecycleHelper.StopAsync`），统一封装"安全停止监控循环"的公共逻辑，三处实现共享同一套代码。 |

---

### RD-03 🟡 `BuildSensorMappings` 与 `BuildButtonMappings` 结构高度相似

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Drivers/Vendors/Leadshaine/Sensors/LeadshaineSensorManager.cs`（行 163–211）`Zeye.NarrowBeltSorter.Drivers/Vendors/Leadshaine/IoPanel/LeadshaineIoPanel.cs`（行 186–231） |
| 问题 | 两者均遵循相同的模板：锁外构建 pointMap → 遍历配置 → 校验 Area=Input → 锁内写入映射 → 锁外发布故障。几乎完全平行的重复结构，维护时需同步修改两处。 |
| 优化建议 | 提取泛型模板方法（或将映射构建逻辑上移到公共基类/扩展方法中），接受配置列表和故障发布委托，减少重复代码量。 |

---

### RD-04 🟢 `SafeExecutor.ExecuteAsync` 两个无 CancellationToken 重载缺少 `ConfigureAwait(false)`

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Core/Utilities/SafeExecutor.cs` |
| 行号 | 43–53、58–66 |
| 问题 | 无 `CancellationToken` 参数的两个 `ExecuteAsync` 重载中的 `await actionTask` 和 `await func()` 缺少 `.ConfigureAwait(false)`，而带 `CancellationToken` 的重载（行 84、111）已正确加了 `ConfigureAwait(false)`，不一致。 |
| 优化建议 | 为 `await actionTask` 和 `await func()` 补加 `.ConfigureAwait(false)`，与其他重载保持一致。 |

---

## 四、性能问题

### PF-01 🔴 `ParcelManager.RaiseSafe` 串行阻塞发布导致高延迟

> 同 **LG-02**，此处从性能角度再次强调。

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Parcel/ParcelManager.cs` |
| 行号 | 386–394 |
| 问题 | 每次发布包裹事件时，所有订阅者被逐一同步执行，事件发布者（分拣协调链路）被阻塞，整体吞吐量受最慢订阅者影响，在高频感应触发场景下尤为明显。 |
| 优化建议 | 改用 `_safeExecutor.PublishEventAsync(...)` 将所有订阅者投入线程池并行执行，发布者立即返回，不再等待任何订阅者完成。 |

---

### PF-02 🟡 `SortingTaskDropOrchestrationService` 热路径中每次重新排序小车列表

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Services/SortingTaskDropOrchestrationService.cs` |
| 行号 | 88、208–216 |
| 问题 | 每次感应位小车变化（高频事件）都执行 `_carrierManager.Carriers.Select(x => x.Id).OrderBy(x => x).ToArray()`，这是 O(N log N) 的 LINQ + ToArray 操作，并且 `Carriers` getter 本身也可能触发 `ToList()` 分配。仓库已有 `_cachedCarrierIndexMap` 缓存机制但热路径未复用，造成双重浪费。 |
| 优化建议 | 将排序结果与 `GetOrBuildCarrierIndexMap` 统一缓存（环形小车建环后结构不变），热路径直接复用缓存结果，避免每次重新排序和分配数组。 |

---

### PF-03 🟡 `ZhiQianChuteManager` 属性每次访问都构建新集合

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Drivers/Vendors/ZhiQian/ZhiQianChuteManager.cs` |
| 行号 | 91–121 |
| 问题 | `Chutes`（ToList）、`TargetChuteIds`（ToHashSet）、`LockedChuteIds`（ToHashSet）、`ChuteConfigurationSnapshot`（ToDictionary）四个属性每次访问都新建集合，若上层服务在每次小车感应时都读取这些属性，会产生持续的 GC 压力。 |
| 优化建议 | 对于不可变的配置快照（`ChuteConfigurationSnapshot`）使用构造时缓存，对频繁变化的集合（`TargetChuteIds`/`LockedChuteIds`）考虑写时复制策略（`ImmutableHashSet`），减少快照创建频率。 |

---

### PF-04 🟡 `LeadshaineEmcController` 监控热路径频繁 `new IoPointInfo`

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Drivers/Vendors/Leadshaine/Emc/LeadshaineEmcController.cs` |
| 行号 | 397–405 |
| 问题 | 监控循环热路径（50ms 周期）中，每次轮询都对所有监控点执行 `new IoPointInfo { ... }`，N 个监控点每轮分配 N 个堆对象，GC 压力随监控点数量线性增长。 |
| 优化建议 | 将 `IoPointInfo` 改为 `struct`（减少堆分配），或将已有 `IoPointInfo` 对象复用（更新属性值），避免每轮都分配新对象。 |

---

### PF-05 🟡 `LeadshaineSensorManager` / `LeadshaineIoPanel` 监控热路径始终分配临时列表

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Drivers/Vendors/Leadshaine/Sensors/LeadshaineSensorManager.cs`（行 226–227）`Zeye.NarrowBeltSorter.Drivers/Vendors/Leadshaine/IoPanel/LeadshaineIoPanel.cs`（行 242–247） |
| 问题 | 监控循环每轮（即使没有任何状态变化）都 `new List<SensorStateChangedEventArgs>()` / `new List<(...)>()` 创建临时列表，无触发场景下列表为空但仍持续分配，在高频轮询（50ms）下 GC 压力不必要。 |
| 优化建议 | 采用"懒创建"模式：仅在需要添加变化事件时才创建列表（`list ??= new()`），无变化时跳过列表分配；或使用 `ArrayPool<T>` 复用缓冲区。 |

---

### PF-06 🟢 `ZhiQianChuteManager.SetForcedChuteAsync` 持锁期间 LINQ 构建字典

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Drivers/Vendors/ZhiQian/ZhiQianChuteManager.cs` |
| 行号 | 224–228 |
| 问题 | `ForceOpenExclusive` 分支在持有 `_writeLock` 期间通过 LINQ `Where + ToDictionary` 构建 `closeMap`，每次调用都分配新字典，而格口配置在初始化后不变，可以提前缓存。 |
| 优化建议 | 在构造时预计算并缓存"所有格口 DO 索引"的列表，`SetForcedChuteAsync` 持锁期间直接过滤已缓存列表，避免每次 LINQ 分配。 |

---

## 五、未落地日志

### LOG-01 🔴 建环完成关键事件无日志落盘

> 同 **DC-01**，此处从日志规范角度再次强调。

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Services/CarrierLoopGroupingHostedService.cs` |
| 行号 | 130 |
| 问题 | `Console.WriteLine("建环完成")` 仅输出到控制台，不落盘，违反"所有业务日志必须落盘到文件"规范，建环完成是关键业务里程碑，必须有可追溯的日志记录。 |
| 优化建议 | 替换为 `_logger.LogInformation("建环完成，小车数量={Count}", _currentRingCarrierIds.Count)`。 |

---

### LOG-02 🔴 `InfraredSensorCarrierManager.ThrowIfDisposed` 异常无日志

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Services/Carrier/InfraredSensorCarrierManager.cs` |
| 行号 | 278–281 |
| 问题 | `ThrowIfDisposed()` 仅抛出 `ObjectDisposedException`，不记录日志，违反"所有异常都必须输出日志"规范。与 `LocalSystemStateManager.cs` 中有日志记录的 `ThrowIfDisposed` 实现不一致。 |
| 优化建议 | 在抛出异常前添加 `_logger.LogError("InfraredSensorCarrierManager 已被释放，拒绝操作。")`，确保异常路径有迹可查。 |

---

### LOG-03 🟡 `InfraredSensorCarrierManager.DisposeAsync` 释放时异常无保护与日志

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Services/Carrier/InfraredSensorCarrierManager.cs` |
| 行号 | 240–258 |
| 问题 | `DisposeAsync` 循环调用每个 `carrier.Dispose()` 时没有 try-catch，若某个小车 Dispose 抛出异常，会中断后续小车的资源释放并丢失日志，导致资源泄漏难以诊断。 |
| 优化建议 | 对每次 `carrier.Dispose()` 添加 try-catch，捕获异常后记录 `_logger.LogError(ex, "释放小车资源失败 CarrierId={CarrierId}", carrier.Id)` 并继续处理后续小车。 |

---

### LOG-04 🟡 `IoMonitoringHostedService.StopAsync` 停止链路异常无日志

> 同 **LG-07**，此处从日志规范角度再次强调。

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Services/Hosted/IoMonitoringHostedService.cs` |
| 行号 | 95–100 |
| 问题 | 正常停止路径（`StopAsync`）无 try-catch，任意步骤异常都不会有日志记录即向上传播，违反"所有异常路径都必须记录日志并落盘"规范。 |
| 优化建议 | 参照同文件 `CleanupAfterStartupFailureAsync` 的模式，为每个停止步骤添加独立的 try-catch 和错误日志。 |

---

### LOG-05 🟡 `DetectMissedChute` 静默跳过包裹不存在的异常路径

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Execution/Services/SortingTaskDropOrchestrationService.cs` |
| 行号 | 291–293 |
| 问题 | `if (!_parcelManager.TryGet(parcelId, out var parcel) \|\| parcel.TargetChuteId <= 0)` 两种情况均静默 `continue`，不区分"包裹不存在（异常状态）"和"包裹无目标格口（正常状态）"，前者属于异常路径，缺少 Warning 级别日志将给问题排查带来困难。 |
| 优化建议 | 拆分两种情况：`TryGet` 返回 false 时记录 `LogWarning("落格检测时包裹不存在 ParcelId={ParcelId} CarrierId={CarrierId}", ...)`；`TargetChuteId <= 0` 时可静默跳过（正常状态）。 |

---

### LOG-06 🟢 `LeadshaineEmcController.SetStatus` 缺少日志记录

> 同 **LG-08**，此处从日志规范角度再次强调。

| 属性 | 内容 |
|---|---|
| 文件 | `Zeye.NarrowBeltSorter.Drivers/Vendors/Leadshaine/Emc/LeadshaineEmcController.cs` |
| 行号 | 428–441 |
| 问题 | 状态机切换仅依靠事件订阅者记录，若订阅者缺失或异常，关键状态变化（断链、故障等）将无迹可查。 |
| 优化建议 | 在 `SetStatus` 方法中直接添加 `_logger.LogInformation` 记录状态转换，不依赖外部订阅者。 |

---

## 六、优先级汇总

| 编号 | 类别 | 优先级 | 文件 | 简述 |
|---|---|---|---|---|
| LG-02 / PF-01 | 逻辑+性能 | 🔴 高 | `ParcelManager.cs` | 事件串行阻塞，应改为并行非阻塞发布 |
| LG-01 | 逻辑 | 🔴 高 | `CarrierLoopGroupingHostedService.cs` | 任意状态变化无条件清空建环数据 |
| DC-01 / LOG-01 | 多余代码+日志 | 🔴 高 | `CarrierLoopGroupingHostedService.cs` | `Console.WriteLine` 遗留调试代码 |
| RD-01 | 冗余代码 | 🔴 高 | `SortingTaskDropOrchestrationService.cs` + `InfraredSensorCarrierManager.cs` | `WrapIndex` 影分身，提取到 `CircularValueHelper` |
| LG-08 / LOG-06 | 逻辑+日志 | 🟡 中 | `LeadshaineEmcController.cs` | EMC 状态切换无日志 |
| DC-03 | 多余代码 | 🟡 中 | `SortingTaskDropOrchestrationService.cs` | 缓存字段与热路径脱节 |
| LG-03 | 逻辑 | 🟡 中 | `SortingTaskOrchestrationService.cs` | 双重 `UnsubscribeEvents` 冗余调用 |
| LG-07 / LOG-04 | 逻辑+日志 | 🟡 中 | `IoMonitoringHostedService.cs` | 停止链路无异常保护和日志 |
| LOG-02 | 日志 | 🟡 中 | `InfraredSensorCarrierManager.cs` | `ThrowIfDisposed` 无日志 |
| LOG-05 | 日志 | 🟡 中 | `SortingTaskDropOrchestrationService.cs` | 静默跳过包裹不存在的异常路径 |
| PF-02 | 性能 | 🟡 中 | `SortingTaskDropOrchestrationService.cs` | 热路径每次重新排序小车列表 |
| PF-03 | 性能 | 🟡 中 | `ZhiQianChuteManager.cs` | 属性每次访问新建集合 |
| PF-04 | 性能 | 🟡 中 | `LeadshaineEmcController.cs` | 监控热路径频繁 `new IoPointInfo` |
| PF-05 | 性能 | 🟡 中 | `LeadshaineSensorManager.cs` + `LeadshaineIoPanel.cs` | 热路径始终分配临时列表 |
| RD-02 | 冗余代码 | 🟡 中 | `LeadshaineIoPanel.cs` + `LeadshaineSensorManager.cs` | 停止逻辑影分身 |
| LG-04 | 逻辑 | 🟡 中 | `SortingTaskOrchestrationService.cs` | Peek/Delay/Dequeue 缺乏原子性 |
| LG-05 | 逻辑 | 🟡 中 | `ZhiQianChuteManager.cs` | 多重锁嵌套顺序不统一 |
| LG-06 | 逻辑 | 🟡 中 | `SortingTaskDropOrchestrationService.cs` | `DetectMissedChute` 两段遍历无原子性 |
| DC-02 | 多余代码 | 🟢 低 | `CarrierLoopGroupingHostedService.cs` | `GetCurrentCarrierId` 无意义包装 |
| DC-04 | 多余代码 | 🟢 低 | `ParcelManager.cs` | 冗余 `?.` null 条件运算符 |
| DC-05 | 多余代码 | 🟢 低 | `ZhiQianChuteManager.cs` | 对不可变字典的多余加锁 |
| DC-06 | 多余代码 | 🟢 低 | `InfraredSensorCarrierManager.cs` | `_loadedCarrierIds` dead field |
| RD-03 | 冗余代码 | 🟢 低 | `LeadshaineSensorManager.cs` + `LeadshaineIoPanel.cs` | 映射构建逻辑高度相似 |
| RD-04 | 冗余代码 | 🟢 低 | `SafeExecutor.cs` | 缺少 `ConfigureAwait(false)` 不一致 |
| LOG-03 | 日志 | 🟢 低 | `InfraredSensorCarrierManager.cs` | `DisposeAsync` 释放异常无保护与日志 |
| PF-06 | 性能 | 🟢 低 | `ZhiQianChuteManager.cs` | 持锁期间 LINQ 构建字典 |

---

## 七、后续可完善点

1. **单元测试补充**：针对 LG-01（建环重置逻辑）、LG-02（事件并行发布）、PF-02（缓存命中率）添加针对性单元测试，确保优化后行为符合预期。
2. **CircularValueHelper 扩展**：`WrapIndex` 合并后，可进一步将其他环形拓扑辅助逻辑集中到该工具类，如 `GetCounterClockwiseValue` 系列方法。
3. **EMC 监控点位对象池**：若 `IoPointInfo` 改为 struct 不可行（需实现接口），可考虑引入 `ObjectPool<IoPointInfo>` 复用实例，配合 NLog 对象消耗频率限制保持日志性能。
4. **锁规范文档化**：对 `ZhiQianChuteManager` 的多重锁增加注释说明"锁获取顺序"，避免未来维护引入死锁。
